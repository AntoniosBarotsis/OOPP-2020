package nl.tudelft.oopp.demo.controllers.startscreen;

import org.junit.jupiter.api.Test;

public class StartControllerTest {

    @Test
    void buttonCreateTest(){
        /* Manual test plan to be carried out when testing if everything works correctly.
        Test plan for testing if opening the create window works.
        1. Launch the start application.
        2. Press the create button
        3. Check if it opens the create stage
         */
    }

    @Test
    void buttonJoinTest(){
        /* Manual test plan to be carried out when testing if everything works correctly.
        Test plan for testing if opening the join window works.
        1. Launch the start application.
        2. Press the join button
        3. Check if it opens the join stage
         */
    }
}
