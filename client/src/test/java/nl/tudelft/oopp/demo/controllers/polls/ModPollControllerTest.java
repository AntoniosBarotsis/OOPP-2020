package nl.tudelft.oopp.demo.controllers.polls;

import org.junit.jupiter.api.Test;

class ModPollControllerTest {

    /**
     * Follow the manual test plan for fetching polls from server first.
     */
    @Test
    void loadData() {
        /* Manual test plan to be carried out when testing if everything works correctly.
        Test plan for testing if loading a poll works:
        1. Upon launching the status should be visible on the left.
        2. The number of participants in the middle.
        3. The date the poll was created on the right.
        4. The text of the poll should be visible in the centre of the view.
        5. Button "Options" should not visible if the poll is closed and results are not shown.
         */
    }

    /**
     * Follow the manual test plan for fetching polls from server first.
     */
    @Test
    void buttonOptionsClicked() {
        /* Manual test plan to be carried out when testing if everything works correctly.
        Test plan for testing if pressing "Options" works:
        1. Upon pressing the button, a new poll window should launch.
         */
    }
}